<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">
   <changeSet id="002-users" author="founder">

        <createTable tableName="users">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true"/>
            </column>

            <column name="school_id" type="BIGINT">
                <constraints nullable="true"/>
            </column>

            <column name="username" type="VARCHAR(100)">
                <constraints unique="true" nullable="false"/>
            </column>

            <column name="password_hash" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="role" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>

            <column name="is_active" type="BOOLEAN"
                    defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMPTZ"
                    defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- FK: users.school_id → schools.id -->
        <addForeignKeyConstraint
                constraintName="fk_users_school_id"
                baseTableName="users"
                baseColumnNames="school_id"
                referencedTableName="schools"
                referencedColumnNames="id"
                onDelete="SET NULL"/>

        <!-- ✅ CHECK constraint via SQL (Liquibase-version safe) -->
        <sql>
            ALTER TABLE users
            ADD CONSTRAINT chk_user_role_school
            CHECK (
                (role = 'SCHOOL' AND school_id IS NOT NULL)
                OR
                (role IN ('OFFICER','ADMIN') AND school_id IS NULL)
            );
        </sql>

    </changeSet>

</databaseChangeLog>