<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

  
    <changeSet id="005-alerts" author="founder">

        <createTable tableName="alerts">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true"/>
            </column>

            <column name="school_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="meal_upload_id" type="BIGINT">
                <constraints nullable="true"/>
            </column>

            <column name="alert_type" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <column name="severity" type="VARCHAR(10)">
                <constraints nullable="false"/>
            </column>

            <column name="message" type="TEXT"/>

            <column name="is_resolved" type="BOOLEAN"
                    defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMPTZ"
                    defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- FK: alerts.school_id → schools.id -->
        <addForeignKeyConstraint
                constraintName="fk_alerts_school"
                baseTableName="alerts"
                baseColumnNames="school_id"
                referencedTableName="schools"
                referencedColumnNames="id"
                onDelete="CASCADE"/>

        <!-- FK: alerts.meal_upload_id → meal_uploads.id -->
        <addForeignKeyConstraint
                constraintName="fk_alerts_meal_upload"
                baseTableName="alerts"
                baseColumnNames="meal_upload_id"
                referencedTableName="meal_uploads"
                referencedColumnNames="id"
                onDelete="SET NULL"/>

        <!-- CHECK constraint via SQL (Liquibase-version safe) -->
        <sql>
            ALTER TABLE alerts
            ADD CONSTRAINT chk_alerts_severity
            CHECK (severity IN ('LOW','MEDIUM','HIGH'));
        </sql>

    </changeSet>

</databaseChangeLog>