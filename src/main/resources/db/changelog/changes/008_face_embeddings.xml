<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">
   
   
     <changeSet id="008-face-embeddings" author="founder">

        <createTable tableName="face_embeddings">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true"/>
            </column>

            <column name="school_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="student_ref" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <!-- PostgreSQL binary data -->
            <column name="embedding" type="BYTEA">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMPTZ"
                    defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- FK: face_embeddings.school_id → schools.id -->
        <addForeignKeyConstraint
                constraintName="fk_face_embeddings_school"
                baseTableName="face_embeddings"
                baseColumnNames="school_id"
                referencedTableName="schools"
                referencedColumnNames="id"
                onDelete="CASCADE"/>

    </changeSet>

</databaseChangeLog>